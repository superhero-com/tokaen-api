name: Staging deploy (multi-server)
on:
  workflow_dispatch:
    inputs:
      VERSION:
        description: "Version to deploy to staging"
        default: "staging"
        required: false
        type: string
  workflow_call:
    inputs:
      VERSION:
        description: "Version to deploy to staging"
        default: "staging"
        required: false
        type: string
jobs:
  deploy_wordcraft:
    name: wordcraft
    uses: ./.github/workflows/ssh_deploy.yaml
    with:
      VERSION: ${{ inputs.VERSION }}
      CONTAINER_NAME: "staging-wordcraftfun-api-mainnet"
    secrets:
      AE_NETWORK_ID: "ae_mainnet"
      API_HOST_PORT: "3033"
      DB_DATABASE: "api"
      DEPLOY_HOST: "api.stag.wordcraft.fun"
      DEPLOY_KEY: ${{ secrets.STAG_DEPLOY_KEY }}
      DEPLOY_USERNAME: ${{ secrets.STAG_DEPLOY_USERNAME }}
      DB_USER: ${{ secrets.STAG_DB_USER }}
      DB_PASSWORD: ${{ secrets.STAG_DB_PASSWORD }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      DOCKERHUB_REPO: ${{ secrets.DOCKERHUB_REPO }}
      TRENDING_TAGS_API_KEY: ${{ secrets.STAG_TRENDING_TAGS_API_KEY }}
  # Uncomment when ready to deploy to trendminer staging
  # deploy_trendminer:
  #   name: trendminer
  #   uses: ./.github/workflows/ssh_deploy.yaml
  #   with:
  #     VERSION: ${{ inputs.VERSION }}
  #     CONTAINER_NAME: "staging-trendminerfun-api-mainnet"
  #   secrets:
  #     AE_NETWORK_ID: "ae_mainnet"
  #     API_HOST_PORT: "3033"
  #     DB_DATABASE: "api"
  #     DEPLOY_HOST: "api.stag.trendminer.fun"
  #     DEPLOY_KEY: ${{ secrets.STAG_TRENDMINERFUN_DEPLOY_KEY }}
  #     DEPLOY_USERNAME: ${{ secrets.STAG_DEPLOY_USERNAME }}
  #     DB_USER: ${{ secrets.STAG_DB_USER }}
  #     DB_PASSWORD: ${{ secrets.STAG_DB_PASSWORD }}
  #     DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  #     DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
  #     DOCKERHUB_REPO: ${{ secrets.DOCKERHUB_REPO }}
